{% extends 'base.html.twig' %}

{% block title %}Hello DashboardController!{% endblock %}

{% block body %}

<h1 class="text-muted text-center">Mon tableau de board</h1>
<hr>
<section class="container">
    <h2 class="text-muted text-center">Mes articles</h2>
    <a class="btn btn-dark mb-2" href="{{path('article')}}">Ajouter un article</a>
    <table class="table">
        <thead>
          <tr>
            <th scope="col">Id</th>
            <th scope="col">Titre</th>
            <th scope="col">Sous-titre</th>
            <th scope="col">Description</th>
            <th scope="col">Illustration</th>
            <th scope="col">Options</th>
          </tr>
        </thead>
        <tbody>
            {% for article in articles %}
          <tr>
            <th>{{article.id}}</th>
            <td>{{article.title}}</td>
            <td>{{article.subtitle}}</td>
            <td title="{{article.description}}">{{article.description | length > 50 ? article.description|slice(0, 50)}}... </td>
            <td><img style="width:100px;" src="{{article.picture}}" alt=""></td>
            <td>
                <a href=""><i class="fa fa-trash"></i></a> |  <a href="{{path('edit_article',{'id': article.id })}}"><i class="fa fa-edit"></i></a> 
            </td>
          </tr>
            {% endfor %}
        </tbody>
      </table>
      <hr>
      <h2 class="text-muted text-center">Mes utilisateurs</h2>
      <table class="table">
          <thead>
            <tr>
              <th scope="col">Id</th>
              <th scope="col">Nom</th>
              <th scope="col">Prénom</th>
              <th scope="col">Email</th>
            </tr>
          </thead>
          <tbody>
              {% for user in users %}
            <tr>
              <th>{{user.id}}</th>
              <td>{{user.lastname}}</td>
              <td>{{user.firstname}}</td>
              <td>{{user.email}}</td>
              <td>
                  <a href=""><i class="fa fa-trash"></i></a></a> 
              </td>
            </tr>
              {% endfor %}
          </tbody>
        </table>
</section>


{% endblock %}
